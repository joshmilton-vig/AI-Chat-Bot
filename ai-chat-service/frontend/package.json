{
  "name": "ai-chat-widget",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=iife --global-name=VividAssistant --outfile=dist/main.js --minify",
    "dev": "esbuild src/index.ts --bundle --format=iife --global-name=VividAssistant --outfile=dist/main.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.6.2"
  }
}
